class GameOpcion {
  int opcion; //numero de la opcion
  bool matched; //variable para saber si ya fue seleccionada
  bool selected; //variable para saber si no fue seleccionada

  GameOpcion(
      {required this.opcion, required this.matched, required this.selected}); //constructor
}
